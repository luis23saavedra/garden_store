Instalación de paquetes para la ejecución del proyecto:
Este sistema permite ingresos como cliente donde puede revisar la tienda y como administrador donde puede crear productos, categorias, clientes.
pip install pillow 
pip install django-seed 
pip install cx_oracle
pip install django-crispy-forms
pip install djangorestframework
Base de datos especificar xe Oracle usuario= c##prueba3 contraseña =123
Crear super usuario con nombre administrador para ingresar como administrador
Para ingresar como cliente basta con registrarse en el botón registrate
Se debe crear una categoria antes de crear una producto a través de admin y crear productoa que pueden ser a través de adminn a través de la web.
el sistema permite simular una compra, no está implementado el sistema de pago.
